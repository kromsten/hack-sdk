const FIRST_MNEMONIC = import.meta.env.FIRST_MNEMONIC;
const FIRST_ENDPOINT = import.meta.env.FIRST_ENDPOINT;
const FIRST_CHAIN_ID = import.meta.env.FIRST_CHAIN_ID;
const FIRST_TOKEN    = import.meta.env.FIRST_TOKEN;
const FIRST_PREFIX   = import.meta.env.FIRST_PREFIX;

const SECOND_MNEMONIC = import.meta.env.SECOND_MNEMONIC;
const SECOND_ENDPOINT = import.meta.env.SECOND_ENDPOINT;
const SECOND_CHAIN_ID = import.meta.env.SECOND_CHAIN_ID;
const SECOND_TOKEN    = import.meta.env.SECOND_TOKEN;
const SECOND_PREFIX   = import.meta.env.SECOND_PREFIX;


export {
    FIRST_MNEMONIC,
    FIRST_ENDPOINT,
    FIRST_CHAIN_ID,
    FIRST_TOKEN,
    FIRST_PREFIX,
    SECOND_MNEMONIC,
    SECOND_ENDPOINT,
    SECOND_CHAIN_ID,
    SECOND_TOKEN,
    SECOND_PREFIX,
};